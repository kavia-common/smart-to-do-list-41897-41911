# Vite environment variables for the Smart To‑Do (Vue 3 + Vite) frontend

# API base URL for backend requests. If left empty, the app uses localStorage.
# Example for local backend-node (default port 4000):
VITE_API_BASE=http://localhost:4000

# Alternative backend URL variable (fallback if VITE_API_BASE is not set).
VITE_BACKEND_URL=

# Optional app and websocket URLs (unused by default).
VITE_FRONTEND_URL=
VITE_WS_URL=

# Dev/build settings
VITE_NODE_ENV=development
VITE_ENABLE_SOURCE_MAPS=true
VITE_PORT=3000
VITE_TRUST_PROXY=
VITE_LOG_LEVEL=
VITE_HEALTHCHECK_PATH=
VITE_FEATURE_FLAGS=
VITE_EXPERIMENTS_ENABLED=

# Notes:
# - To use localStorage only, unset VITE_API_BASE and VITE_BACKEND_URL.
# - To use the backend API, set VITE_API_BASE (preferred) or VITE_BACKEND_URL.
# - tasks.api.ts and http.ts will automatically detect the base URL via env.
